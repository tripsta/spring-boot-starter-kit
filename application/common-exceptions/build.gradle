apply plugin: "maven-publish"
jar.enabled = true

bootJar {
    mustRunAfter tasks.clean
    mainClassName = 'common-exceptions'
    archiveName 'common-exceptions.jar'
}

dependencies {
    compile(
            [group: 'junit', name: 'junit', version: "${jUnitVersion}"]
    )
}
